/**
 * Select UI component.
 *
 * A thin wrapper that re-exports the `Select` component from `@inkjs/ui`.
 * Renders a single-choice selection list in the terminal, allowing the user
 * to navigate and pick one option from a set.
 *
 * @module
 */
export { Select } from '@inkjs/ui'
export type { SelectProps } from '@inkjs/ui'
