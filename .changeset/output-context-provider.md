---
'@kidd-cli/core': minor
---

Output component now reads the OutputStore from screen context automatically. The `store` prop and `OutputProps` type have been removed — use `<Output />` with no props inside `screen()` components.
