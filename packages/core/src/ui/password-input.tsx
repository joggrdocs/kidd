/**
 * PasswordInput UI component.
 *
 * A thin wrapper that re-exports the `PasswordInput` component from `@inkjs/ui`.
 * Renders a masked text input in the terminal for securely entering sensitive
 * values such as passwords or tokens.
 *
 * @module
 */
export { PasswordInput } from '@inkjs/ui'
export type { PasswordInputProps } from '@inkjs/ui'
