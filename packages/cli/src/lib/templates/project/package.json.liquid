{
  "name": "{{ name }}",
  "version": "0.0.0",
  "description": "{{ description }}",
  "keywords": [],
  "license": "MIT",
  "bin": {
    "{{ name }}": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "kidd build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@kidd-cli/core": "^0.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@kidd-cli/cli": "^0.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  }
}
