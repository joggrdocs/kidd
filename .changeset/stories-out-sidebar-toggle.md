---
'@kidd-cli/core': minor
'@kidd-cli/cli': minor
---

Add `--out` option to stories command that renders components to stdout (supports `Group/Variant` format), `b` hotkey to toggle sidebar visibility with borderless preview, collapse sidebar groups by default with folder icons, and `defaults` field on `stories()` for non-editable fixed context props
