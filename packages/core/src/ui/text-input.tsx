/**
 * TextInput UI component.
 *
 * A thin wrapper that re-exports the `TextInput` component from `@inkjs/ui`.
 * Renders a text input field in the terminal with support for placeholders,
 * default values, and autocomplete suggestions.
 *
 * @module
 */
export { TextInput } from '@inkjs/ui'
export type { TextInputProps } from '@inkjs/ui'
