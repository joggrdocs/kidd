{
  "name": "@examples/authenticated-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx api/server.ts & API_PID=$!; kidd dev --cwd cli; kill $API_PID 2>/dev/null",
    "api": "tsx api/server.ts",
    "dev": "kidd dev --cwd cli",
    "build": "kidd build --cwd cli",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@kidd-cli/core": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@kidd-cli/cli": "workspace:*",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
