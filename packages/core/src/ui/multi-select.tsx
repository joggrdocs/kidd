/**
 * MultiSelect UI component.
 *
 * A thin wrapper that re-exports the `MultiSelect` component from `@inkjs/ui`.
 * Renders a multi-choice selection list in the terminal, allowing the user to
 * select multiple options from a set.
 *
 * @module
 */
export { MultiSelect } from '@inkjs/ui'
export type { MultiSelectProps } from '@inkjs/ui'
