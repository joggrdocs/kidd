---
'@kidd-cli/core': minor
---

Add crash handlers that catch uncaught exceptions and unhandled rejections, displaying a clean fatal error message and writing a debug log to `/tmp` instead of showing raw runtime stack traces.
