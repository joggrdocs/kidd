/**
 * ConfirmInput UI component.
 *
 * A thin wrapper that re-exports the `ConfirmInput` component from `@inkjs/ui`.
 * Renders a confirm/cancel prompt in the terminal, allowing the user to accept
 * or reject an action.
 *
 * @module
 */
export { ConfirmInput } from '@inkjs/ui'
export type { ConfirmInputProps } from '@inkjs/ui'
