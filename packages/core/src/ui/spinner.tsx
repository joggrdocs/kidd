/**
 * Spinner UI component.
 *
 * A thin wrapper that re-exports the `Spinner` component from `@inkjs/ui`.
 * Displays an animated spinner with an optional label, useful for indicating
 * loading or in-progress operations in the terminal.
 *
 * @module
 */
export { Spinner } from '@inkjs/ui'
export type { SpinnerProps } from '@inkjs/ui'
